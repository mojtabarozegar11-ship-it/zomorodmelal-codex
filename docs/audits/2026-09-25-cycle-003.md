# REAL ENGINEERING — Cycle 003

Date: 2026-09-25
Scope: GITHUB-ONLY

## Evidence baseline

- Repository: `mojtabarozegar11-ship-it/zomorodmelal-codex`
- Candidate baseline: `fix/restore-project-and-host-bridge`
- Candidate HEAD audited: `46a56cdb88f0ec04701d538123bcf4badb798652`
- Engineering branch: `evolution/cycle-003-ci-evidence-hardening`

## Scope boundary

Only repository branches, commits, pull requests, source, tests, Actions, documentation and configuration were inspected.

Production server, cPanel runtime, production database, DNS and live files were not inspected or changed. Any claim requiring those systems is:

**UNVERIFIED — OUTSIDE GITHUB SCOPE**

## Repository state

The current `main` branch is not the populated Django implementation baseline. The candidate branch contains the Django runtime, application tree, tests and CI/CD configuration required for deeper engineering audit.

This cycle does not merge the candidate into `main`; canonical-lineage resolution remains an owner-governed decision.

## Finding: CI deployment-check evidence gap

### REASON

The candidate CI workflow runs:

`python manage.py check --deploy`

with `DJANGO_DEBUG=False`, while `config.settings` defaults `DJANGO_HSTS_SECONDS` to `0`.

That makes the deploy-security check dependent on an unstated environment value and can produce a deployment-security warning even though the workflow intends to validate the production-safe configuration.

### CHANGE

In `.github/workflows/ci.yml`, the CI-only environment for the Django system/deploy checks now explicitly sets:

`DJANGO_HSTS_SECONDS=31536000`

This is test configuration only. It does not modify production settings, secrets, DNS, cPanel or deployment commands.

### TEST

The workflow definition itself was re-read after the change and the new variable is present in both Django check steps.

Actual GitHub Actions execution has not yet been observed for the new commit.

**Automated execution result: UNKNOWN**

### VERIFY

Verified repository-level properties:

- Change is confined to the CI workflow.
- No production workflow was modified.
- No secret value was added.
- No source runtime behavior was changed.
- No destructive operation was performed.
- Branch remains separate from `main`.

## Additional audit observations

1. `production-deploy.yml` is a privileged boundary and remains approval-gated by workflow inputs and application-level deployment gate language. No production action was triggered.
2. `deployment/host_adapter.py` keeps host deployment disabled unless explicitly enabled and rejects shell metacharacter tokens before subprocess execution.
3. `host_bridge/files.php` enforces bearer authentication, HTTPS checks, root confinement and path traversal rejection, but its actual host deployment/security posture cannot be certified from GitHub alone.
4. `config/site_target.json` explicitly records owner approval as required and real changes as disabled for its discovery mode.
5. The repository contains multiple CI workflows with overlapping Django checks. Consolidation is deferred because it would alter CI behavior without evidence that the duplication is defective.

## Score gate

No 100/100 certification.

Reasons:

- Canonical mainline remains unresolved.
- New CI execution evidence is not yet available.
- Production state remains **UNVERIFIED — OUTSIDE GITHUB SCOPE**.
- Vital criteria therefore remain UNKNOWN.

## Next cycle

1. Obtain CI execution evidence from the proposed PR.
2. Inspect CI failures, if any, using job output rather than assumptions.
3. Continue source-level security audit of authentication, authorization, policy/approval and agent execution boundaries.
4. Map tests to critical runtime paths.
5. Compare implementation against `docs/REWRITE_100_PERCENT_MASTER_SPEC.md` where the specification is available.
6. Resolve canonical lineage before any mainline integration.
7. Re-score only from accumulated evidence.
