# Automatic deployment trigger

A push to `release/cpanel-final-20260921` triggers `.github/workflows/deploy-cpanel-ssh.yml` automatically.

This file is intentionally kept as a lightweight trigger commit so the deployment pipeline can be exercised without changing application code.
