# Deprecated: legacy orchestrator

This orchestration tree is retired from the production path.

Use:

    python -m master_agent

New production integrations must use master_agent.CanonicalMasterAgent and the
autonomous_core package.
