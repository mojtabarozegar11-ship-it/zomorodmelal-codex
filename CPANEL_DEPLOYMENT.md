# Server deployment retired

This document is retained only as historical reference.

Zomorod Melal production is an installable Android application. cPanel, Passenger, LiteSpeed, and hosted Django deployment are not production targets.

Use \`worker_mojtaba/android\` and the Android CI workflow for the production build path.
