# Deprecated: auto_agent/runtime

This runtime is retired from the production path.

Use:

    python -m master_agent

Do not add new production dependencies, launchers, schedulers or integrations
under this directory. Existing modules are retained temporarily for migration
and compatibility analysis.
