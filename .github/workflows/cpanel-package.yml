name: DEPRECATED - cPanel Packaging Disabled

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "cPanel packaging is intentionally disabled."
          echo "The product is an Android application, not a hosted web application."
          exit 1
