name: DEPRECATED - Server Deployment Disabled

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "Server/cPanel deployment is intentionally disabled."
          echo "Production target is the installable Android application."
          exit 1
