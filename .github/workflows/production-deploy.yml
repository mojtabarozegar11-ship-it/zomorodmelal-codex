name: Deprecated server deployment gate

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "This legacy production-deploy workflow is intentionally disabled."
          echo "Use cPanel package/deploy workflows for the Django host runtime."
          echo "Use the Android production workflow for APK/AAB validation."
          exit 1
